#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAXN 100001

int main() {
    long long i, j, k;
    char string[MAXN];
    fgets(string, MAXN, stdin);
    scanf("%lld", &k);

    return 0;
}
