#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAXN 205
#define MAXK 35

int main() {
    int k, n, i, chain = 1;
    while(scanf("%d %d", &n, &k)) {
        if(n == 0 && k == 0) break;
        for(i = 0; i < n; ++i) {
             
        }
        printf("Chain %d\n", chain);
        ++chain;
        for(i = 1; i <= k; ++i) {
            printf("Depot %d at restaurant serves restaurants\n", k);
        }
        printf("Total distance sum = \n");
    }
    return 0;
}