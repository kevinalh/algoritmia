#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int n, k, i, j;
    long long ways;
    while(scanf("%d %d", &n, &k) && (n+k != 0))
        for(i = 0; i <= n; ++i) {
            for(j = 0; j <= n; ++j) {
                
            }
        }
        printf("%lld\n", ways);
    }
    return 0;
}
